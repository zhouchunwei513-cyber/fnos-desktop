# 内置 MPV 播放器 · 菜单与字幕功能说明（v1.29）

本版本**不改变**原有软件逻辑、操作方式与窗口方式，只在现有「应用内嵌入 MPV」播放器的
**中文右键菜单**上把控制做完整：播放/暂停、进度跳转、倍速、音轨（按名称选择）、字幕
（内置轨/本地字幕/在线搜索下载/样式微调）、画中画。

## 一、操作入口

在视频画面上**右键**即弹出全中文菜单：

| 菜单 | 功能 |
|------|------|
| 播放 / 暂停（空格） | 播放暂停 |
| 进度跳转 | ±5秒 / ±1分钟 / ±10分钟 / 回到开头 |
| 播放速度 | 0.25 / 0.5 / 0.75 / 1.0 / 1.25 / 1.5 / 2.0 |
| 音轨 / 声音 | **按名称+语言列出所有音轨**直接切换；静音、音量、音频延迟 |
| 字幕 | 开关、加载本地字幕、在线搜索（中/英）、**内置字幕轨按名称选择**、延迟/位置/字号 |
| 画面 | 全屏、画面比例、截图、亮度/对比度 |
| 画中画（小窗置顶） | 进入/退出小窗，小窗固定在右下角、始终置顶、可自由拖动 |
| 停止并返回网页 | 关闭 MPV，回到网页 |

## 二、字幕功能

### 1. 内置字幕轨
视频自带的多条字幕会在「字幕 → 内置字幕轨」里按语言/标题列出，点击即可切换；
也可「不显示内置字幕」。

### 2. 加载本地字幕
「字幕 → 加载本地字幕文件…」→ 主进程弹出文件选择对话框（仅主进程能调系统对话框），
过滤 `srt / ass / ssa / vtt / sub`，选中后以**绝对路径** `sub-add` 加载进 MPV 并自动显示。

### 3. 在线搜索字幕（OpenSubtitles）
1. 「字幕 → 在线搜索字幕（中文）/（英文）」；搜索词默认取当前媒体文件名，可在结果为空时换关键词重试。
2. 搜索完成后菜单出现「▶ 在线搜索结果」子菜单，每条显示「名称 [语言] 下载量」，点击即下载。
3. 下载后自动解压并加载：
   - 单文件（`.gz` 直链）→ 直接加载；
   - `.zip` 内含多个字幕 → 主进程弹窗列出文件让你**手动选择**（不会默认加载第一个）。
4. 字幕缓存目录（程序退出不清理）：
   `%APPDATA%\fn-electron\cache\subtitle`
   （代码用 `app.getPath('appData')` 拼接，**不硬编码** `%appdata%`。）
5. 限流/超时/失败都会在画面上以中文提示，不静默失败。

## 三、画中画（PiP）

- 进入：菜单「画中画（小窗置顶）」。窗口缩为 420px 宽、固定在视频区右下角，
  **始终置顶**（切到微信等外部窗口也保持悬浮），并脱离视频区跟随，可自由拖动。
- 再点一次即退出，恢复为贴合视频区并跟随主窗移动/缩放。

## 四、架构与关键约束（开发者）

- **播放层不变**：仍是 `child_process.spawn` 启动内置 `mpv.exe`，无边框置顶覆盖窗 +
  Windows 命名管道 JSON-RPC 控制；没有引入 node-libmpv，也没有改用 ffmpeg 软解。
- **新增本地助手服务 `mpv-helper.js`**：在 `127.0.0.1` 随机端口起极简 HTTP 服务，
  仅监听回环、带一次性 token（经环境变量 `FNOS_MPV_HELPER_PORT/TOKEN` 在 spawn mpv 时注入）。
  MPV 内的菜单脚本 `mpv-config/scripts/fnos-menu.lua` 通过 Windows 自带
  `%WINDIR%\System32\curl.exe`（`subprocess` 异步调用，不卡播放）请求助手：
  - `POST /subtitle/search`       在线搜索
  - `POST /subtitle/download`     下载→解压(gz/zlib，zip/adm-zip)→多文件弹窗选择→`sub-add`
  - `POST /subtitle/open-dialog`  本地字幕文件对话框 → `sub-add`
  - `POST /pip/toggle`            画中画开关
- **主进程职责**：命名管道通信、在线字幕网络请求、zip/gz 解压、文件对话框、`sub-add`，
  全部在主进程完成；菜单脚本只做 UI 与 curl 触发。
- **wid/覆盖层限制**：MPV 画面是独立原生窗直接 GPU 绘制，网页 DOM 浮不到它上面，
  因此控制 UI 仍走 MPV 自身中文右键菜单（本版本不做网页悬浮控制条）。
- **字幕路径必须绝对路径**：传给 mpv 的媒体/字幕一律绝对路径，相对路径会加载失败。
- **第三方包**：仅新增 `adm-zip`（zip 解压），其余使用 Node/Electron 原生 API；
  未引入任何 mpv-ipc 第三方封装库。

## 五、本地运行

```bash
pnpm install
pnpm start      # 或 pnpm dev
```

## 六、mpv 放置与打包

- 开发态：`mpv/mpv.exe`（含依赖 dll）与 `mpv-config/`（mpv.conf + scripts/fnos-menu.lua）。
- 打包态（electron-builder）：`extraResources` 已把 `mpv/`、`mpv-config/` 一并输出；
  运行时从 `process.resourcesPath/mpv/mpv.exe` 解析。
- `package.json` 已声明依赖 `adm-zip`，并把 `mpv-helper.js` 加入 `files`。
- 构建：`pnpm build`（electron-builder 24.x，适配 Electron 22）。
